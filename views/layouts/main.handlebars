<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Hello Bulma!</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
</head>
<body>
    <section class="section">
        <div class="container">
            <h1 class="title">
                Anime Fusion
            </h1>
            <p class="subtitle">
                Welcome to our <strong> Anime Fusion </strong> Homepage!
            </p>
            <form class="box is-small is-forth-quarter">
                <div class="field is-small">
                    <label class="label is-small">Email</label>
                    <div class="control is-small">
                        <input class="input is-small" type="email" placeholder="e.g. alex@example.com">
                    </div>
                </div>

                <div class="field is-small">
                    <label class="label is-small">Password</label>
                    <div class="control is-small">
                        <input class="input is-small" type="password" placeholder="********">
                    </div>
                </div>
                <button class="button is-primary is-small">Sign in</button>
            </form>
            <nav class="navbar is-primary" role="navigation" aria-label="main navigation">
                <div class="container">
                    <div class="navbar-brand">
                        <a class="navbar-item" href="#">
                            Navigation Section
                        </a>
                    </div>
                </div>
            </nav>

            <section class="section">
                <div class="container">
                    <div class="columns">
                        <!-- Left Section - Top 5 Favorite Anime -->
                        <div class="column is-one-quarter">
                            <h2 class="title has-text-centered notification is-danger">Top 5 Favorite Anime</h2>
                            <ol>
                                {{#each top5Anime}}
                                <li class="notification has-background-warning-light has-text-centered is-size-2">
                                    <a href="#" onclick="showPopup('{{title}}', '{{description}}', '{{image}}')">{{title}}</a>
                                </li>
                                {{/each}}
                            </ol>
                        </div>

                        <section class="section">
                            <div class="container">
                                <div class="columns is-multiline">
                                    <!-- Anime Section -->
                                    {{#each animeSections}}
                                    <div class="column is-one-third">
                                        <div class="box">
                                            <p class="title is-size-2">{{title}}</p>
                                            <img src="{{image}}" alt="{{title}}">
                                            <p class="subtitle">{{description}}</p>
                                            <!-- Like and Dislike buttons and counters add button for watchlist -->
                                            <div class="field is-grouped">
                                                <p class="control">
                                                    <button class="button is-success is-medium is-fullwidth" onclick="likeAnime({{id}})">Like</button>
                                                </p>
                                                <p class="control">
                                                    <button class="button is-danger is-medium is-fullwidth" onclick="dislikeAnime({{id}})">Dislike</button>
                                                </p>
                                            </div>
                                            <p class="control">
                                                <button class="button is-info is-medium is-fullwidth">Watchlist</button>
                                            </p>
                                        </div>
                                    </div>
                                    {{/each}}
                                </div>
                            </div>
                        </section>

                        <!-- Pop-up Window -->
                        <div id="popup" class="modal">
                            <div class="modal-background"></div>
                            <div class="modal-content">
                                <div class="box">
                                    <button class="delete" onclick="closePopup()"></button>
                                    <h2 id="popupTitle" class="title"></h2>
                                    <p id="popupDescription" class="subtitle"></p>
                                    <img id="popupImage" src="" alt="Anime Image">
                                </div>
                            </div>
                        </div>

                        <!-- Add a button to navigate to the watchlist page -->
                        <div class="has-text-centered">
                            <a class="button is-primary" href="watchlist.html">Go to Watchlist</a>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Anime Rating Form -->
            <!-- <div class="box">
                <form id="rating-form">
                    <div class="field">
                        <label class="label">Anime Title</label>
                        <div class="control">
                            <input class="input" type="text" id="anime-title" required>
                        </div>
                    </div>
                    <div class="field">
                        <label class="label">Your Rating (out of 10)</label>
                        <div class="control">
                            <input class="input" type="number" id="anime-rating" min="1" max="10" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <button class="button is-primary" type="submit">Submit Rating</button>
                        </div>
                    </div>
                </form>
            </div> -->

            <!-- Display Ratings -->
            <div id="ratings-list">
                <!-- Ratings will be displayed here -->
            </div>
        </div>
        <div id="anime-title"></div>
    </section>
    </div>
    </section>
    <script src="./public/css/script.js"></script>
    <script src="./server.js"></script>
</body>
</html>
